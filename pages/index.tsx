import React from 'react';
import TodoList from '../src/components/TodoList';

const Index = () => {
	return (
		<React.StrictMode>
			<TodoList />
		</React.StrictMode>
	);
};

export default Index;
